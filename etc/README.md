# Source Configuration

Source-controlled topology profile templates used by `scripts/sdkwork-topology.mjs scaffold-profiles`.
Applications materialize these templates under their own `etc/topology/` directories. This reusable
framework root does not own application environment values or secrets.
